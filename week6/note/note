1、建立动态共享库
	1.1与静态库有什么区别
		静态库，在运行前直接装载进去，不再依赖静态库的存在，动态库则不同，不直接装载进去没，而更像是个指针存在方便更新操作。
	1.2生成动态库的代码
	gcc -share -fPIC -o 目标文件名称(xxxx.so) xx.c mm.c
	本地调用动态库的方法
	gcc -o 目标文件名称 xxx.c ./xxxx.so
	外部调用动态库的方法
	gcc -o 目标文件 xx.c ../mmm/xxx.so(动态库路径) -I../mm（动态库父目录）
